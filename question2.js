// Build a simple loop, inside a function, that takes an array and prints the value of the array to the console.


// for loop
function arrayNum(row) {
    for(let i = 0; i < row; i++ );
    return row = ["grape", "fruit", "guava"]
}console.log(arrayNum())

// while loop
let row = ["grape", "fruit", "guava"]
while(row.length <= 3){
    console.log(row);
    row++;
}
